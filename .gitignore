# Variables de entorno — NUNCA subir esto
.env
.env.local

# Cookies de YouTube — contienen sesión de cuenta de YouTube, NUNCA subir esto
youtube-cookie.json

# Configuración de auditoría — datos del servidor y credenciales, NUNCA subir esto
audit-config.json

# Dependencias — se reinstalan con npm install
node_modules/

# Archivos de sistema de macOS y Windows
.DS_Store
Thumbs.db

# Logs de desarrollo y producción
*.log

# Archivos de prueba y desarrollo
check.js